{
  "name": "cajuncookbook",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install --include=dev && ./node_modules/.bin/ng build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
